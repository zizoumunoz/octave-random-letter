%This script finds the area of a rectangle


rWidth = input('Enter a width: ');%asks for the width


rLength = input('Enter a width: ');%asks for the height

%calculates the area
rArea = rLength*rWidth;

fprintf('The area of your rectangle is %3.2f\n', rArea);
